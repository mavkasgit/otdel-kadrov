# Журнал приказов - План реализации

> **Для агентной работы:** Для выполнения использовать superpowers:subagent-driven-development или superpowers:executing-plans

**Goal:** Добавить в db_engine методы для работы с журналом приказов: чтение, добавление, автонумерация, поиск сотрудников

**Architecture:** Расширение ExcelDatabase новыми методами для журнала событий. Единый поиск сотрудника по ФИО или Таб.№

**Tech Stack:** Python, pandas, xlwings, regex

---

## Файлы

- Modify: `core/db_engine.py` - добавление методов
- Test: `tests/test_db_engine_order_log.py` - тесты

---

## Задачи

### Task 1: Добавить константы в settings.py

**Files:**
- Modify: `settings.py`

- [ ] **Шаг 1: Добавить константы**

Добавить в `settings.py`:
```python
SHEET_ORDER_LOG = "Журнал событий"

ORDER_LOG_COLUMNS = [
    "Номер приказа",
    "Тип события",
    "Дата создания",
    "ФИО",
    "Таб. №",
    "Путь к файлу"
]

ORDER_TYPE_CODES = {
    "Прием на работу": "П",
    "Увольнение": "У",
    "Отпуск трудовой": "ОТ",
    "Отпуск за свой счет": "ОТБ",
    "Больничный": "Б",
    "Перевод": "ПЕР",
    "Продление контракта": "ПК"
}
```

- [ ] **Шаг 2: Commit**

```bash
git add settings.py
git commit -m "feat: add order log constants"
```

---

### Task 2: Метод get_order_log()

**Files:**
- Modify: `core/db_engine.py:510` - добавить после delete_vacation

- [ ] **Шаг 1: Написать тест**

```python
# tests/test_db_engine_order_log.py
import pytest
from unittest.mock import MagicMock, patch

def test_get_order_log_returns_dataframe(mock_db):
    """Тест: get_order_log возвращает DataFrame"""
    # Arrange
    mock_sheet = MagicMock()
    mock_sheet.used_range.value = [
        ["Номер приказа", "Тип события", "Дата создания", "ФИО", "Таб. №", "Путь к файлу"],
        ["001-П", "Прием на работу", "2024-01-15", "Иванов Иван Иванович", 123, "приказы/test.docx"]
    ]
    mock_db.workbook.sheets = {"Журнал событий": mock_sheet}
    mock_db._sheets_cache = {"Журнал событий": mock_sheet}
    
    # Act
    result = mock_db.get_order_log()
    
    # Assert
    assert len(result) == 1
    assert result.iloc[0]["Номер приказа"] == "001-П"
```

- [ ] **Шаг 2: Запустить тест - ожидаем FAIL**

Run: `pytest tests/test_db_engine_order_log.py::test_get_order_log_returns_dataframe -v`
Expected: FAIL - method not defined

- [ ] **Шаг 3: Реализовать метод**

```python
def get_order_log(self, order_type: Optional[str] = None) -> pd.DataFrame:
    """
    Чтение журнала приказов
    
    Args:
        order_type: Опциональный фильтр по типу события
        
    Returns:
        DataFrame с записями журнала
    """
    try:
        sheet = self._get_sheet(settings.SHEET_ORDER_LOG)
        logger.debug(f"Reading order log from sheet: {settings.SHEET_ORDER_LOG}")
        
        used_range = sheet.used_range
        data = used_range.value
        
        if not data or len(data) < 2:
            logger.warning("Order log sheet is empty")
            return pd.DataFrame(columns=settings.ORDER_LOG_COLUMNS)
        
        headers = data[0]
        rows = data[1:]
        
        df = pd.DataFrame(rows, columns=headers)
        
        # Convert date column
        if "Дата создания" in df.columns:
            df = self._convert_dates(df, ["Дата создания"])
        
        df = df.where(pd.notna(df), None)
        
        # Apply filter if specified
        if order_type and "Тип события" in df.columns:
            df = df[df["Тип события"] == order_type]
            logger.debug(f"Filtered by order type: {order_type}, rows: {len(df)}")
        
        logger.info(f"Retrieved {len(df)} order log entries")
        return df
        
    except Exception as e:
        logger.error(f"Error reading order log: {e}")
        raise DatabaseConnectionError(f"Error reading order log: {str(e)}") from e
```

- [ ] **Шаг 4: Запустить тест - ожидаем PASS**

Run: `pytest tests/test_db_engine_order_log.py::test_get_order_log_returns_dataframe -v`
Expected: PASS

- [ ] **Шаг 5: Commit**

```bash
git add core/db_engine.py tests/test_db_engine_order_log.py
git commit -m "feat: add get_order_log method"
```

---

### Task 3: Метод _get_type_code()

**Files:**
- Modify: `core/db_engine.py` - добавить вспомогательный метод

- [ ] **Шаг 1: Написать тест**

```python
def test_get_type_code_returns_correct_code(mock_db):
    """Тест: _get_type_code возвращает правильный код"""
    # Act
    code = mock_db._get_type_code("Прием на работу")
    assert code == "П"
    
    code = mock_db._get_type_code("Отпуск трудовой")
    assert code == "ОТ"
    
    code = mock_db._get_type_code("Увольнение")
    assert code == "У"
```

- [ ] **Шаг 2: Запустить тест - ожидаем FAIL**

Run: `pytest tests/test_db_engine_order_log.py::test_get_type_code_returns_correct_code -v`
Expected: FAIL - method not defined

- [ ] **Шаг 3: Реализовать метод**

```python
def _get_type_code(self, order_type: str) -> str:
    """
    Получение кода типа из справочника
    
    Args:
        order_type: Название типа из справочника
        
    Returns:
        Код типа (2 символа max)
        
    Raises:
        ValueError: Если тип не найден в справочнике
    """
    code = settings.ORDER_TYPE_CODES.get(order_type)
    if code is None:
        raise ValueError(f"Unknown order type: {order_type}")
    return code
```

- [ ] **Шаг 4: Запустить тест - ожидаем PASS**

Run: `pytest tests/test_db_engine_order_log.py::test_get_type_code_returns_correct_code -v`
Expected: PASS

- [ ] **Шаг 5: Commit**

```bash
git add core/db_engine.py
git commit -m "feat: add _get_type_code method"
```

---

### Task 4: Метод get_next_order_number()

**Files:**
- Modify: `core/db_engine.py` - добавить метод

- [ ] **Шаг 1: Написать тест**

```python
def test_get_next_order_number_empty_log(mock_db):
    """Тест: следующий номер для пустого журнала"""
    # Arrange - empty log
    mock_sheet = MagicMock()
    mock_sheet.used_range.value = [["Номер приказа", "Тип события"]]
    mock_db.workbook.sheets = {"Журнал событий": mock_sheet}
    mock_db._sheets_cache = {"Журнал событий": mock_sheet}
    
    # Act
    next_num = mock_db.get_next_order_number("Прием на работу")
    
    # Assert
    assert next_num == "001-П"

def test_get_next_order_number_existing(mock_db):
    """Тест: следующий номер с существующими записями"""
    # Arrange
    mock_sheet = MagicMock()
    mock_sheet.used_range.value = [
        ["Номер приказа", "Тип события"],
        ["001-П", "Прием на работу"],
        ["002-П", "Прием на работу"]
    ]
    mock_db.workbook.sheets = {"Журнал событий": mock_sheet}
    mock_db._sheets_cache = {"Журнал событий": mock_sheet}
    
    # Act
    next_num = mock_db.get_next_order_number("Прием на работу")
    
    # Assert
    assert next_num == "003-П"
```

- [ ] **Шаг 2: Запустить тест - ожидаем FAIL**

Run: `pytest tests/test_db_engine_order_log.py::test_get_next_order_number_empty_log -v`
Expected: FAIL

- [ ] **Шаг 3: Реализовать метод**

```python
def get_next_order_number(self, order_type: str) -> str:
    """
    Получение следующего номера для типа приказа
    
    Args:
        order_type: Тип события (например "Прием на работу")
        
    Returns:
        Следующий номер, например "001-П"
        
    Raises:
        ValueError: Если тип события не найден в справочнике
    """
    # Validate type and get code
    type_code = self._get_type_code(order_type)
    
    # Get existing orders of this type
    df = self.get_order_log(order_type)
    
    if df.empty:
        # Start from 001
        next_num = 1
    else:
        # Extract numbers from "XXX-КОД" pattern
        import re
        numbers = []
        for order_num in df["Номер приказа"]:
            match = re.match(r'(\d+)-', str(order_num))
            if match:
                numbers.append(int(match.group(1)))
        
        if numbers:
            next_num = max(numbers) + 1
        else:
            next_num = 1
    
    return f"{next_num:03d}-{type_code}"
```

- [ ] **Шаг 4: Запустить тест - ожидаем PASS**

Run: `pytest tests/test_db_engine_order_log.py::test_get_next_order_number_empty_log tests/test_db_engine_order_log.py::test_get_next_order_number_existing -v`
Expected: PASS

- [ ] **Шаг 5: Commit**

```bash
git add core/db_engine.py
git commit -m "feat: add get_next_order_number method"
```

---

### Task 5: Метод find_employee()

**Files:**
- Modify: `core/db_engine.py` - добавить метод

- [ ] **Шаг 1: Написать тест**

```python
def test_find_employee_by_tab_number(mock_db):
    """Тест: поиск по табельному номеру"""
    # Arrange
    mock_sheet = MagicMock()
    mock_sheet.used_range.value = [
        ["Таб. №", "ФИО", "Подразделение"],
        [123, "Иванов Иван Иванович", "Завод КТМ"],
        [456, "Петров Петр Петрович", "Основное"]
    ]
    mock_db.workbook.sheets = {"Сотрудники": mock_sheet}
    mock_db._sheets_cache = {"Сотрудники": mock_sheet}
    
    # Act
    result = mock_db.find_employee("123")
    
    # Assert
    assert result is not None
    assert result["ФИО"] == "Иванов Иван Иванович"

def test_find_employee_by_fio(mock_db):
    """Тест: поиск по ФИО"""
    # Act
    result = mock_db.find_employee("Петров")
    
    # Assert
    assert result is not None
    assert result["Таб. №"] == 456
```

- [ ] **Шаг 2: Запустить тест - ожидаем FAIL**

Run: `pytest tests/test_db_engine_order_log.py::test_find_employee_by_tab_number -v`
Expected: FAIL

- [ ] **Шаг 3: Реализовать метод**

```python
def find_employee(self, search_value: str) -> Optional[Dict]:
    """
    Единый поиск сотрудника по ФИО или Таб.№
    
    Args:
        search_value: Строка поиска (ФИО или Таб.№)
        
    Returns:
        Dict с данными сотрудника или None если не найден
    """
    try:
        df = self.get_employees()
        
        if df.empty:
            return None
        
        # Try to parse as number (for tab number)
        try:
            tab_num = int(search_value)
            # Search by exact tab number match
            matches = df[df["Таб. №"] == tab_num]
            
            if not matches.empty:
                return matches.iloc[0].to_dict()
        except ValueError:
            pass
        
        # Search by partial FIO match (case-insensitive)
        search_lower = search_value.lower().strip()
        matches = df[df["ФИО"].astype(str).str.lower().str.contains(search_lower, na=False)]
        
        if matches.empty:
            logger.warning(f"Employee not found: {search_value}")
            return None
        
        if len(matches) > 1:
            logger.warning(f"Multiple matches for '{search_value}': {len(matches)} found, using first")
        
        return matches.iloc[0].to_dict()
        
    except Exception as e:
        logger.error(f"Error searching employee '{search_value}': {e}")
        return None
```

- [ ] **Шаг 4: Запустить тест - ожидаем PASS**

Run: `pytest tests/test_db_engine_order_log.py::test_find_employee_by_tab_number tests/test_db_engine_order_log.py::test_find_employee_by_fio -v`
Expected: PASS

- [ ] **Шаг 5: Commit**

```bash
git add core/db_engine.py
git commit -m "feat: add find_employee method"
```

---

### Task 6: Метод add_order_log()

**Files:**
- Modify: `core/db_engine.py` - добавить метод

- [ ] **Шаг 1: Написать тест**

```python
def test_add_order_log(mock_db):
    """Тест: добавление записи в журнал"""
    # Arrange
    mock_sheet = MagicMock()
    mock_sheet.used_range.value = [
        ["Номер приказа", "Тип события", "Дата создания", "ФИО", "Таб. №", "Путь к файлу"]
    ]
    mock_sheet.used_range.last_cell.row = 1
    mock_db.workbook.sheets = {"Журнал событий": mock_sheet}
    mock_db._sheets_cache = {"Журнал событий": mock_sheet}
    
    # Mock employee search
    mock_db.find_employee = MagicMock(return_value={
        "ФИО": "Иванов Иван Иванович",
        "Таб. №": 123
    })
    
    # Act
    order_number = mock_db.add_order_log({
        "order_type": "Прием на работу",
        "search_value": "123",
        "file_path": "приказы/test.docx"
    })
    
    # Assert
    assert order_number == "001-П"
    # Verify write was called
    mock_sheet.range.assert_called()
```

- [ ] **Шаг 2: Запустить тест - ожидаем FAIL**

Run: `pytest tests/test_db_engine_order_log.py::test_add_order_log -v`
Expected: FAIL

- [ ] **Шаг 3: Реализовать метод**

```python
def add_order_log(self, order_data: Dict) -> str:
    """
    Добавление записи в журнал приказов
    
    Args:
        order_data: {
            'order_type': str,      # Обязательно
            'search_value': str,    # Единое поле поиска (ФИО или Таб.№)
            'file_path': str        # Опционально
        }
        
    Returns:
        Номер созданного приказа
        
    Raises:
        ValueError: Если сотрудник не найден или тип события некорректен
    """
    # Validate order type
    order_type = order_data.get("order_type")
    if not order_type:
        raise ValueError("order_type is required")
    
    # Validate type exists
    self._get_type_code(order_type)
    
    # Find employee
    search_value = order_data.get("search_value")
    if not search_value:
        raise ValueError("search_value is required")
    
    employee = self.find_employee(search_value)
    if employee is None:
        raise ValueError(f"Сотрудник не найден: {search_value}")
    
    # Get next order number
    order_number = self.get_next_order_number(order_type)
    
    # Prepare row data
    sheet = self._get_sheet(settings.SHEET_ORDER_LOG)
    
    # Get last row
    last_row = sheet.used_range.last_cell.row
    next_row = last_row + 1
    
    # Current date for order
    today = date.today()
    
    # Write row
    new_row = [
        order_number,
        order_type,
        today,
        employee.get("ФИО"),
        employee.get("Таб. №"),
        order_data.get("file_path")
    ]
    
    sheet.range(f"A{next_row}").value = new_row
    
    logger.info(f"Added order log entry: {order_number} for {employee.get('ФИО')}")
    return order_number
```

- [ ] **Шаг 4: Запустить тест - ожидаем PASS**

Run: `pytest tests/test_db_engine_order_log.py::test_add_order_log -v`
Expected: PASS

- [ ] **Шаг 5: Commit**

```bash
git add core/db_engine.py
git commit -m "feat: add add_order_log method"
```

---

## Checklist самопроверки

- [ ] Spec coverage: Все требования из спецификации покрыты
- [ ] Placeholder scan: Нет TBD/TODO
- [ ] Type consistency: Названия методов консистентны
- [ ] Все задачи имеют конкретные шаги с кодом

---

## Выбор способа выполнения

**Plan complete and saved to `.kiro/plans/2026-03-29-order-log.md`. Two execution options:**

1. **Subagent-Driven (recommended)** - Я запускаю отдельные подзадачи, проверяю между ними
2. **Inline Execution** - Выполняю все задачи в текущей сессии

Какой вариант выбираешь?
